node_modules/
dist/
.DS_Store
*.log

# Local env / secrets — NEVER commit these
.env
.env.local
.env.*.local

# Oversized unused asset — exceeds GitHub's 100MB limit
public/models/billboard-2.glb
_pictures_originals/
_models_originals/
